const App ={currentTab:'overview',async init(){try{document.body.classList.add('loading');await DataProcessor.loadData();this.initFilters();this.initPWAComponents();this.showDataSourceIndicator();this.initTabs();this.initSalaryToggles();this.initMapControls();this.initOffersCTA();Charts.init();Timeline.init();DossierMetiers.init();this.updateStats();this.updateMetrics();Charts.update();window.addEventListener('dataFiltered',()=>{this.updateStats();this.updateMetrics();Charts.update();Timeline.update();DossierMetiers.update();if (this.currentTab === 'map'){FranceMap.update();}});window.addEventListener('dataRefreshed',()=>{console.log('[App] Data refreshed,updating UI...');this.updateStats();this.updateMetrics();Charts.update();Timeline.update();DossierMetiers.update();if (this.currentTab === 'map'){FranceMap.update();}this.showDataSourceIndicator();});if (window.Router){Router.init();}document.body.classList.remove('loading');console.log('[App] Application initialized successfully');}catch(error){console.error('[App] Failed to initialize:',error);document.body.classList.remove('loading');if (window.ErrorToast){ErrorToast.init();if (!navigator.onLine){ErrorToast.showNetworkError(8000);}else{ErrorToast.showDataError(8000);}}document.body.classList.add('js-error');const contentArea = document.querySelector('.content-area');if (contentArea){contentArea.innerHTML = `
<div style="text-align:center;padding:50px;max-width:500px;margin:0 auto;">
<h2 style="color:#1e293b;margin-bottom:16px;">Erreur de chargement</h2>
<p style="color:#64748b;margin-bottom:24px;">${navigator.onLine
? 'Impossible de charger les données. Veuillez réessayer.':'Vous êtes hors-ligne et aucune donnée n\'est en cache.'}</p>
<button onclick="window.location.reload()" style="padding:12px 24px;background:#2563eb;color:white;border:none;border-radius:8px;cursor:pointer;font-size:16px;">
Rafraîchir la page
</button>
</div>
`;}}},initPWAComponents(){if (window.UpdateToast){UpdateToast.init();}if (window.BottomSheet){BottomSheet.init();const data = DataProcessor.getData();if (data?.departments){BottomSheet.setDepartmentData(data.departments);}}if (window.SwipeNavigation){SwipeNavigation.init();}console.log('[App] PWA components initialized');},showDataSourceIndicator(){const source = DataProcessor.getDataSource?.()|| 'unknown';if (source === 'cache'){console.log('[App] Using cached data');}},initFilters(){const data = DataProcessor.getData();const jobFamilyContainer = document.getElementById('filter-job-family');if (jobFamilyContainer){const families = JobClassifier.getAllFamilyNames().sort((a,b)=> a.localeCompare(b,'fr'));jobFamilyContainer.innerHTML = families.map(family => `
<label>
<input type="checkbox" value="${family}" class="filter-job-family-cb">
<span style="color:${JobClassifier.getColor(family)};">●</span> ${family}</label>
`).join('');jobFamilyContainer.querySelectorAll('.filter-job-family-cb').forEach(cb =>{cb.addEventListener('change',()=> this.applyFilters());});}const contractContainer = document.getElementById('filter-contract');if (contractContainer){const extractDuration =(contract)=>{const monthMatch = contract.match(/(\d+)\s*Mois/i);if (monthMatch)return parseInt(monthMatch[1])* 30;const dayMatch = contract.match(/(\d+)\s*Jours?/i);if (dayMatch)return parseInt(dayMatch[1]);return 0;};const contracts = Object.keys(data.byContractType).sort((a,b)=>{if (a === 'CDI')return -1;if (b === 'CDI')return 1;const isCDDA = a.startsWith('CDD');const isCDDB = b.startsWith('CDD');if (isCDDA && isCDDB){return extractDuration(b)- extractDuration(a);}if (isCDDA)return -1;if (isCDDB)return 1;return a.localeCompare(b,'fr');});contractContainer.innerHTML = contracts.map(type => `
<label>
<input type="checkbox" value="${type}" class="filter-contract-cb">
${type}</label>
`).join('');contractContainer.querySelectorAll('.filter-contract-cb').forEach(cb =>{cb.addEventListener('change',()=> this.applyFilters());});}this.initScrollIndicators();this.initDepartmentAutocomplete(data);const resetBtn = document.getElementById('reset-filters');if (resetBtn){resetBtn.addEventListener('click',()=>{document.querySelectorAll('.filter-job-family-cb,.filter-contract-cb').forEach(cb => cb.checked = false);const deptInput = document.getElementById('dept-search');const deptDropdown = document.getElementById('dept-dropdown');if (deptInput)deptInput.value = '';if (deptDropdown)deptDropdown.hidden = true;this.clearSelectedDepartments();DataProcessor.resetFilters();});}window.dispatchEvent(new CustomEvent('filtersReady'));console.log('[App] Filters initialized and ready');},initScrollIndicators(){document.querySelectorAll('.checkbox-group').forEach(group =>{const wrapper = group.parentElement;if (!wrapper?.classList.contains('checkbox-group-wrapper'))return;const checkScroll =()=>{const isAtEnd = group.scrollHeight - group.scrollTop <= group.clientHeight + 5;const isScrollable = group.scrollHeight > group.clientHeight;if (!isScrollable || isAtEnd){wrapper.classList.add('scrolled-end');}else{wrapper.classList.remove('scrolled-end');}};group.addEventListener('scroll',checkScroll);setTimeout(checkScroll,100);});},selectedDepts:new Set(),initDepartmentAutocomplete(data){const input = document.getElementById('dept-search');const dropdown = document.getElementById('dept-dropdown');const chipsContainer = document.getElementById('dept-chips');if (!input || !dropdown || !chipsContainer)return;const departments = Object.entries(data.departments);let highlightedIndex = -1;const renderChips =()=>{chipsContainer.innerHTML = Array.from(this.selectedDepts).map(code =>{const name = data.departments[code];return `
<span class="dept-chip" data-code="${code}">
${name}(${code})<button class="dept-chip-remove" aria-label="Retirer ${name}">&times;</button>
</span>
`;}).join('');chipsContainer.querySelectorAll('.dept-chip-remove').forEach(btn =>{btn.addEventListener('click',(e)=>{const chip = e.target.closest('.dept-chip');const code = chip.dataset.code;this.selectedDepts.delete (code);renderChips();this.applyFilters();});});};const renderDropdown =(query)=>{if (!query || query.length < 1){dropdown.hidden = true;return;}const matches = departments
.filter(([code,name])=>
!this.selectedDepts.has(code)&&(code.toLowerCase().includes(query)||
name.toLowerCase().includes(query))).slice(0,8);if (matches.length === 0){dropdown.innerHTML = '<div class="dept-no-results">Aucun résultat</div>';}else{dropdown.innerHTML = matches.map(([code,name],i)=> `
<div class="dept-option ${i === highlightedIndex ? 'highlighted':''}"
data-code="${code}"
data-index="${i}"
role="option">
<span class="dept-option-code">${code}</span>
${name}</div>
`).join('');}dropdown.hidden = false;dropdown.querySelectorAll('.dept-option').forEach(opt =>{opt.addEventListener('click',()=> selectDept(opt.dataset.code));});};const selectDept =(code)=>{this.selectedDepts.add(code);input.value = '';dropdown.hidden = true;highlightedIndex = -1;renderChips();this.applyFilters();};let debounceTimer;input.addEventListener('input',(e)=>{clearTimeout(debounceTimer);debounceTimer = setTimeout(()=>{highlightedIndex = -1;renderDropdown(e.target.value.toLowerCase());},100);});input.addEventListener('keydown',(e)=>{const options = dropdown.querySelectorAll('.dept-option');if (e.key === 'ArrowDown'){e.preventDefault();highlightedIndex = Math.min(highlightedIndex + 1,options.length - 1);updateHighlight(options);}else if (e.key === 'ArrowUp'){e.preventDefault();highlightedIndex = Math.max(highlightedIndex - 1,0);updateHighlight(options);}else if (e.key === 'Enter' && highlightedIndex >= 0){e.preventDefault();const code = options[highlightedIndex]?.dataset.code;if (code)selectDept(code);}else if (e.key === 'Escape'){dropdown.hidden = true;highlightedIndex = -1;}});const updateHighlight =(options)=>{options.forEach((opt,i)=>{opt.classList.toggle('highlighted',i === highlightedIndex);});};document.addEventListener('click',(e)=>{if (!e.target.closest('.dept-autocomplete')){dropdown.hidden = true;}});input.addEventListener('focus',()=>{if (input.value.length >= 1){renderDropdown(input.value.toLowerCase());}});},getSelectedDepartments(){return Array.from(this.selectedDepts);},clearSelectedDepartments(){this.selectedDepts.clear();const chipsContainer = document.getElementById('dept-chips');if (chipsContainer){chipsContainer.innerHTML = '';}},applyFilters(){const jobFamilies = Array.from(document.querySelectorAll('.filter-job-family-cb:checked')).map(cb => cb.value);const contractTypes = Array.from(document.querySelectorAll('.filter-contract-cb:checked')).map(cb => cb.value);const departments = Array.from(this.selectedDepts);DataProcessor.applyFilters({jobFamilies,contractTypes,departments});},initTabs(){const tabs = document.querySelectorAll('.tab-btn');const contents = document.querySelectorAll('.tab-content');tabs.forEach(tab =>{tab.addEventListener('click',()=>{const target = tab.dataset.tab;tabs.forEach(t => t.classList.remove('active'));tab.classList.add('active');contents.forEach(c =>{c.classList.remove('active');if (c.id === `tab-${target}`){c.classList.add('active');}});this.currentTab = target;window.dispatchEvent(new CustomEvent('tabChanged',{detail:{tab:target}}));if (window.SwipeNavigation){SwipeNavigation.setCurrentTab(target);}if (target === 'map' && !FranceMap.svg){FranceMap.init();}if (target === 'sectors'){this.renderSectorTreemap();this.renderSectorTable();}});});},initSalaryToggles(){document.querySelectorAll('[data-salary-type]').forEach(btn =>{btn.addEventListener('click',()=>{document.querySelectorAll('[data-salary-type]').forEach(b => b.classList.remove('active'));btn.classList.add('active');Charts.setSalaryType(btn.dataset.salaryType);});});document.querySelectorAll('[data-salary-period]').forEach(btn =>{btn.addEventListener('click',()=>{document.querySelectorAll('[data-salary-period]').forEach(b => b.classList.remove('active'));btn.classList.add('active');Charts.setSalaryPeriod(btn.dataset.salaryPeriod);});});},initMapControls(){document.querySelectorAll('[data-map-color]').forEach(btn =>{btn.addEventListener('click',()=>{document.querySelectorAll('[data-map-color]').forEach(b => b.classList.remove('active'));btn.classList.add('active');FranceMap.setColorMode(btn.dataset.mapColor);});});},initOffersCTA(){document.querySelectorAll('.btn-cta').forEach(btn =>{btn.addEventListener('click',()=>{const context = btn.dataset.context;let offers = DataProcessor.getOffers(true);let contextLabel = 'Toutes les offres filtrées';if (context === 'with-salary'){offers = offers.filter(o => o.salaryNetMonthly);contextLabel = 'Offres avec salaire renseigné';}OffersModal.show(offers,contextLabel);});});},updateStats(){const data = DataProcessor.getData();const stats = DataProcessor.getStats();document.getElementById('total-offers').textContent = SalaryUtils.formatNumber(stats.total);document.getElementById('offers-with-salary').textContent = SalaryUtils.formatNumber(stats.withSalary);const dateRange = `${data.meta.dateRange.first.slice(5)}- ${data.meta.dateRange.last.slice(5)}`;document.getElementById('date-range').textContent = dateRange;},updateMetrics(){const stats = DataProcessor.getStats();document.getElementById('metric-total').textContent = SalaryUtils.formatNumber(stats.total);document.getElementById('metric-sectors').textContent = Object.keys(stats.bySector).length;document.getElementById('metric-depts').textContent = Object.keys(stats.byDepartment).length;},colorblindPalette:['#0077BB','#EE7733','#009988','#CC3311','#33BBEE','#EE3377','#BBBBBB','#AA3377','#44BB99','#DDCC77','#882255','#332288','#117733','#999933','#CC6677',],sectorColors:{},renderSectorTreemap(){const container = document.getElementById('sector-treemap');if (!container)return;const stats = DataProcessor.getStats();const data = Object.entries(stats.bySector).filter(([name])=> name.toLowerCase()!== 'non précisé').map(([name,d])=>({name,value:d.count})).sort((a,b)=> b.value - a.value).slice(0,20);container.innerHTML = '';const width = container.clientWidth;const height = 400;const svg = d3.select(container).append('svg').attr('width',width).attr('height',height);const root = d3.hierarchy({children:data}).sum(d => d.value);d3.treemap().size([width,height]).padding(2)(root);const color = d3.scaleOrdinal(this.colorblindPalette);this.sectorColors ={};data.forEach((d,i)=>{this.sectorColors[d.name] = color(i);});const nodes = svg.selectAll('g').data(root.leaves()).enter().append('g').attr('transform',d => `translate(${d.x0},${d.y0})`);nodes.append('rect').attr('width',d => d.x1 - d.x0).attr('height',d => d.y1 - d.y0).attr('fill',(d,i)=> color(i)).attr('rx',4).style('cursor','pointer').on('click',(e,d)=>{console.log('Clicked sector:',d.data.name);});nodes.append('foreignObject').attr('x',4).attr('y',4).attr('width',d => Math.max(0,d.x1 - d.x0 - 8)).attr('height',d => Math.max(0,d.y1 - d.y0 - 8)).append('xhtml:div').style('width','100%').style('height','100%').style('display','flex').style('flex-direction','column').style('justify-content','flex-start').style('overflow','hidden').style('color','white').style('font-size','11px').style('line-height','1.2').style('pointer-events','none').html(d =>{const cellWidth = d.x1 - d.x0;const cellHeight = d.y1 - d.y0;if (cellWidth < 50 || cellHeight < 30)return '';const showCount = cellHeight >= 45;return `
<div style="font-weight:500;word-wrap:break-word;overflow-wrap:break-word;">${d.data.name}</div>
${showCount ? `<div style="opacity:0.8;font-size:10px;margin-top:2px;">${d.data.value}offres</div>`:''}`;});},renderSectorTable(){const tbody = document.querySelector('#sector-table tbody');if (!tbody)return;const stats = DataProcessor.getStats();const sectors = Object.entries(stats.bySector).sort((a,b)=> b[1].count - a[1].count).slice(0,15);tbody.innerHTML = sectors.map(([name,data])=>{const color = this.sectorColors[name] || '#BBBBBB';return `
<tr>
<td>
<span class="sector-color-dot" style="background-color:${color}"></span>
${name}</td>
<td>${data.count}</td>
<td>${data.salaryStats ? `${SalaryUtils.formatCurrency(data.salaryStats.median)}<span class="count-badge">(n=${data.salaryStats.count})</span>`:'-'}</td>
</tr>
`;}).join('');}};document.addEventListener('DOMContentLoaded',()=> App.init());const Router =(function (){const config ={basePath:'',defaultTab:'overview',routes:{'/':'overview','/salaires':'salaries','/carte':'map','/secteurs':'sectors','/dossier':'dossier'},tabToPath:{'overview':'/','salaries':'/salaires','map':'/carte','sectors':'/secteurs','dossier':'/dossier'},params:{jobFamilies:'famille',contractTypes:'contrat',departments:'dept'}};let isInitialized = false;let isNavigating = false;function init(){if (isInitialized)return;detectBasePath();const state = parseCurrentURL();setTimeout(()=>{applyState(state,false);},0);window.addEventListener('popstate',handlePopState);window.addEventListener('tabChanged',handleTabChange);window.addEventListener('dataFiltered',handleFilterChange);isInitialized = true;console.log('[Router] Initialized with basePath:',config.basePath);}function detectBasePath(){const pathname = window.location.pathname;const baseEl = document.querySelector('base');if (baseEl && baseEl.href){const baseURL = new URL(baseEl.href);config.basePath = baseURL.pathname.replace(/\/$/,'');return;}if (window.location.hostname.endsWith('.github.io')){const parts = pathname.split('/').filter(Boolean);if (parts.length > 0){const firstPart = '/' + parts[0];if (!config.routes[firstPart]){config.basePath = firstPart;}}}}function parseCurrentURL(){let pathname = window.location.pathname;const params = new URLSearchParams(window.location.search);const redirectedRoute = params.get('__route');if (redirectedRoute){params.delete ('__route');const cleanSearch = params.toString();pathname = decodeURIComponent(redirectedRoute);const cleanURL = config.basePath + pathname +(cleanSearch ? '?' + cleanSearch:'');history.replaceState(null,'',cleanURL);}let routePath = pathname;if (config.basePath && pathname.startsWith(config.basePath)){routePath = pathname.slice(config.basePath.length)|| '/';}routePath = routePath.replace(/\/$/,'')|| '/';const tab = config.routes[routePath] || config.defaultTab;const filters ={jobFamilies:parseArrayParam(params.get(config.params.jobFamilies)),contractTypes:parseArrayParam(params.get(config.params.contractTypes)),departments:parseArrayParam(params.get(config.params.departments))};return{tab,filters};}function parseArrayParam(value){if (!value)return[];return value.split(',').map(v => decodeURIComponent(v.trim())).filter(Boolean);}function encodeArrayParam(arr){if (!arr || arr.length === 0)return null;return arr.map(v => encodeURIComponent(v)).join(',');}function buildURL(tab,filters){const path = config.tabToPath[tab] || '/';const fullPath = config.basePath + path;const params = new URLSearchParams();if (filters.jobFamilies?.length){params.set(config.params.jobFamilies,encodeArrayParam(filters.jobFamilies));}if (filters.contractTypes?.length){params.set(config.params.contractTypes,encodeArrayParam(filters.contractTypes));}if (filters.departments?.length){params.set(config.params.departments,encodeArrayParam(filters.departments));}const queryString = params.toString();return fullPath +(queryString ? '?' + queryString:'');}function navigate(tab,filters = null,replace = false){if (isNavigating)return;isNavigating = true;if (!filters && window.DataProcessor){filters = DataProcessor.filters;}filters = filters ||{jobFamilies:[],contractTypes:[],departments:[]};const url = buildURL(tab,filters);if (replace){history.replaceState({tab,filters},'',url);}else{history.pushState({tab,filters},'',url);}isNavigating = false;}function applyState(state,fromPopState = true){if (isNavigating)return;isNavigating = true;const tabBtn = document.querySelector(`.tab-btn[data-tab="${state.tab}"]`);if (tabBtn && !tabBtn.classList.contains('active')){tabBtn.click();}const hasFilters = state.filters.jobFamilies.length > 0 ||
state.filters.contractTypes.length > 0 ||
state.filters.departments.length > 0;if (hasFilters && window.DataProcessor){DataProcessor.applyFilters(state.filters);syncFiltersToUI(state.filters);}isNavigating = false;}function syncFiltersToUI(filters){const jobFamilyContainer = document.getElementById('filter-job-family');if (jobFamilyContainer){jobFamilyContainer.querySelectorAll('input[type="checkbox"]').forEach(cb =>{cb.checked = filters.jobFamilies.includes(cb.value);});}const contractContainer = document.getElementById('filter-contract');if (contractContainer){contractContainer.querySelectorAll('input[type="checkbox"]').forEach(cb =>{cb.checked = filters.contractTypes.includes(cb.value);});}if (window.BottomSheet && BottomSheet.syncWithSidebar){BottomSheet.syncWithSidebar();}}function handlePopState(event){if (event.state){applyState(event.state);}else{const state = parseCurrentURL();applyState(state);}}function handleTabChange(event){if (isNavigating)return;const tab = event.detail?.tab;if (tab){navigate(tab,null,false);}}function handleFilterChange(event){if (isNavigating)return;const filters = event.detail?.filters;const currentTab = window.App?.currentTab || 'overview';if (filters){navigate(currentTab,filters,true);}}function getShareableURL(){const currentTab = window.App?.currentTab || 'overview';const filters = window.DataProcessor?.filters ||{jobFamilies:[],contractTypes:[],departments:[]};return window.location.origin + buildURL(currentTab,filters);}function setBasePath(path){config.basePath = path.replace(/\/$/,'');}function getCurrentRoute(){return parseCurrentURL();}return{init,navigate,parseCurrentURL,buildURL,getShareableURL,setBasePath,getCurrentRoute};})();window.Router = Router;const Storage =(function (){const DB_NAME = 'CepidInfovizDB';const DB_VERSION = 1;let db = null;const STORES ={OFFERS:'offers',DEPARTMENTS:'departments',META:'meta',PREFERENCES:'preferences'};async function init(){return new Promise((resolve,reject)=>{if (db){resolve(db);return;}const request = indexedDB.open(DB_NAME,DB_VERSION);request.onerror =()=>{console.error('[Storage] Failed to open database:',request.error);reject(request.error);};request.onsuccess =()=>{db = request.result;console.log('[Storage] Database opened successfully');resolve(db);};request.onupgradeneeded =(event)=>{const database = event.target.result;console.log('[Storage] Upgrading database schema');if (!database.objectStoreNames.contains(STORES.OFFERS)){const offersStore = database.createObjectStore(STORES.OFFERS,{keyPath:'id'});offersStore.createIndex('jobFamily','jobFamily',{unique:false});offersStore.createIndex('department','department',{unique:false});offersStore.createIndex('contractType','contractType',{unique:false});offersStore.createIndex('date','date',{unique:false});}if (!database.objectStoreNames.contains(STORES.DEPARTMENTS)){database.createObjectStore(STORES.DEPARTMENTS,{keyPath:'code'});}if (!database.objectStoreNames.contains(STORES.META)){database.createObjectStore(STORES.META,{keyPath:'key'});}if (!database.objectStoreNames.contains(STORES.PREFERENCES)){database.createObjectStore(STORES.PREFERENCES,{keyPath:'key'});}};});}async function saveOffers(offers){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.OFFERS],'readwrite');const store = transaction.objectStore(STORES.OFFERS);store.clear();let count = 0;offers.forEach((offer)=>{store.put(offer);count++;});transaction.oncomplete =()=>{console.log(`[Storage] Saved ${count}offers`);resolve(count);};transaction.onerror =()=>{console.error('[Storage] Failed to save offers:',transaction.error);reject(transaction.error);};});}async function getOffers(){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.OFFERS],'readonly');const store = transaction.objectStore(STORES.OFFERS);const request = store.getAll();request.onsuccess =()=>{resolve(request.result);};request.onerror =()=>{console.error('[Storage] Failed to get offers:',request.error);reject(request.error);};});}async function getOffersByJobFamily(jobFamily){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.OFFERS],'readonly');const store = transaction.objectStore(STORES.OFFERS);const index = store.index('jobFamily');const request = index.getAll(jobFamily);request.onsuccess =()=>{resolve(request.result);};request.onerror =()=>{reject(request.error);};});}async function getOffersByDepartment(department){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.OFFERS],'readonly');const store = transaction.objectStore(STORES.OFFERS);const index = store.index('department');const request = index.getAll(department);request.onsuccess =()=>{resolve(request.result);};request.onerror =()=>{reject(request.error);};});}async function saveDepartments(departments){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.DEPARTMENTS],'readwrite');const store = transaction.objectStore(STORES.DEPARTMENTS);store.clear();Object.entries(departments).forEach(([code,name])=>{store.put({code,name});});transaction.oncomplete =()=>{console.log('[Storage] Saved departments');resolve();};transaction.onerror =()=>{reject(transaction.error);};});}async function getDepartments(){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.DEPARTMENTS],'readonly');const store = transaction.objectStore(STORES.DEPARTMENTS);const request = store.getAll();request.onsuccess =()=>{const result ={};request.result.forEach((dept)=>{result[dept.code] = dept.name;});resolve(result);};request.onerror =()=>{reject(request.error);};});}async function saveMeta(key,value){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.META],'readwrite');const store = transaction.objectStore(STORES.META);store.put({key,value,updatedAt:new Date().toISOString()});transaction.oncomplete =()=>{resolve();};transaction.onerror =()=>{reject(transaction.error);};});}async function getMeta(key){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.META],'readonly');const store = transaction.objectStore(STORES.META);const request = store.get(key);request.onsuccess =()=>{resolve(request.result ? request.result.value:null);};request.onerror =()=>{reject(request.error);};});}async function saveAllMeta(meta){await saveMeta('generatedAt',meta.generatedAt);await saveMeta('dataSource',meta.dataSource);await saveMeta('totalOffers',meta.totalOffers);await saveMeta('offersWithSalary',meta.offersWithSalary);await saveMeta('dateRange',meta.dateRange);}async function getAllMeta(){return{generatedAt:await getMeta('generatedAt'),dataSource:await getMeta('dataSource'),totalOffers:await getMeta('totalOffers'),offersWithSalary:await getMeta('offersWithSalary'),dateRange:await getMeta('dateRange')};}async function savePreference(key,value){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.PREFERENCES],'readwrite');const store = transaction.objectStore(STORES.PREFERENCES);store.put({key,value});transaction.oncomplete =()=>{resolve();};transaction.onerror =()=>{reject(transaction.error);};});}async function getPreference(key,defaultValue = null){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.PREFERENCES],'readonly');const store = transaction.objectStore(STORES.PREFERENCES);const request = store.get(key);request.onsuccess =()=>{resolve(request.result ? request.result.value:defaultValue);};request.onerror =()=>{reject(request.error);};});}async function saveAggregatedData(data){console.log('[Storage] Saving aggregated data...');const startTime = Date.now();await saveOffers(data.offers);await saveDepartments(data.departments);await saveAllMeta(data.meta);await saveMeta('dates',data.dates);await saveMeta('byContractType',data.byContractType);await saveMeta('byJobFamily',data.byJobFamily);const elapsed = Date.now()- startTime;console.log(`[Storage] Saved aggregated data in ${elapsed}ms`);}async function loadAggregatedData(){console.log('[Storage] Loading aggregated data from IndexedDB...');const startTime = Date.now();try{const offers = await getOffers();if (!offers || offers.length === 0){console.log('[Storage] No cached data found');return null;}const data ={meta:await getAllMeta(),dates:await getMeta('dates'),departments:await getDepartments(),offers:offers,byContractType:await getMeta('byContractType'),byJobFamily:await getMeta('byJobFamily')};const elapsed = Date.now()- startTime;console.log(`[Storage] Loaded ${offers.length}offers from IndexedDB in ${elapsed}ms`);return data;}catch(error){console.error('[Storage] Failed to load data:',error);return null;}}async function hasData(){try{await init();return new Promise((resolve)=>{const transaction = db.transaction([STORES.OFFERS],'readonly');const store = transaction.objectStore(STORES.OFFERS);const request = store.count();request.onsuccess =()=>{resolve(request.result > 0);};request.onerror =()=>{resolve(false);};});}catch(error){return false;}}async function getDataVersion(){return getMeta('generatedAt');}async function clearAll(){await init();return new Promise((resolve,reject)=>{const transaction = db.transaction([STORES.OFFERS,STORES.DEPARTMENTS,STORES.META],'readwrite');transaction.objectStore(STORES.OFFERS).clear();transaction.objectStore(STORES.DEPARTMENTS).clear();transaction.objectStore(STORES.META).clear();transaction.oncomplete =()=>{console.log('[Storage] All data cleared');resolve();};transaction.onerror =()=>{reject(transaction.error);};});}async function getStorageEstimate(){if (navigator.storage && navigator.storage.estimate){const estimate = await navigator.storage.estimate();return{usage:estimate.usage,quota:estimate.quota,percentUsed:((estimate.usage / estimate.quota)* 100).toFixed(2)};}return null;}return{init,saveOffers,getOffers,getOffersByJobFamily,getOffersByDepartment,saveDepartments,getDepartments,saveMeta,getMeta,saveAllMeta,getAllMeta,savePreference,getPreference,saveAggregatedData,loadAggregatedData,hasData,getDataVersion,clearAll,getStorageEstimate,STORES};})();window.Storage = Storage;const DataProcessor ={data:null,filteredOffers:null,isOffline:!navigator.onLine,dataSource:'none',filters:{jobFamilies:[],contractTypes:[],departments:[],dateRange:null},async loadData(){console.log('[DataProcessor] Loading data...');this.setupOnlineListeners();if (navigator.onLine){try{const data = await this.fetchFromNetwork();if (data){this.data = data;this.filteredOffers =[...this.data.offers];this.dataSource = 'network';this.saveToStorage(data);return this.data;}}catch(error){console.warn('[DataProcessor] Network fetch failed:',error.message);}}const cachedData = await this.loadFromStorage();if (cachedData){this.data = cachedData;this.filteredOffers =[...this.data.offers];this.dataSource = 'cache';console.log('[DataProcessor] Loaded from IndexedDB cache');return this.data;}throw new Error('No data available. Please connect to the internet.');},async fetchFromNetwork(){console.log('[DataProcessor] Fetching from network...');const response = await fetch('data/aggregated.json');if (!response.ok)throw new Error('Network response not ok');return response.json();},async saveToStorage(data){try{await Storage.saveAggregatedData(data);console.log('[DataProcessor] Data saved to IndexedDB');}catch(error){console.error('[DataProcessor] Failed to save to IndexedDB:',error);}},async loadFromStorage(){try{return await Storage.loadAggregatedData();}catch(error){console.error('[DataProcessor] Failed to load from IndexedDB:',error);return null;}},setupOnlineListeners(){window.addEventListener('online',()=>{console.log('[DataProcessor] Back online');this.isOffline = false;this.updateOfflineIndicator(false);this.refreshIfNeeded();});window.addEventListener('offline',()=>{console.log('[DataProcessor] Gone offline');this.isOffline = true;this.updateOfflineIndicator(true);});this.updateOfflineIndicator(!navigator.onLine);},updateOfflineIndicator(isOffline){const indicator = document.getElementById('offline-indicator');if (indicator){indicator.hidden = !isOffline;}},async refreshIfNeeded(){if (this.dataSource === 'cache'){try{const networkData = await this.fetchFromNetwork();if (networkData){const cachedVersion = this.data.meta?.generatedAt;const newVersion = networkData.meta?.generatedAt;if (newVersion &&(!cachedVersion || newVersion > cachedVersion)){console.log('[DataProcessor] Newer data available,refreshing...');this.data = networkData;this.filteredOffers =[...this.data.offers];this.dataSource = 'network';this.saveToStorage(networkData);window.dispatchEvent(new CustomEvent('dataRefreshed',{detail:{offers:this.filteredOffers}}));}}}catch(error){console.warn('[DataProcessor] Background refresh failed:',error);}}},async hasCachedData(){return Storage.hasData();},getDataSource(){return this.dataSource;},getData(){return this.data;},getOffers(filtered = true){return filtered ? this.filteredOffers:this.data.offers;},applyFilters(newFilters){this.filters ={...this.filters,...newFilters};this.filteredOffers = this.data.offers.filter(offer =>{if (this.filters.jobFamilies.length > 0){if (!this.filters.jobFamilies.includes(offer.jobFamily))return false;}if (this.filters.contractTypes.length > 0){if (!this.filters.contractTypes.includes(offer.contractType))return false;}if (this.filters.departments.length > 0){if (!this.filters.departments.includes(offer.department))return false;}return true;});window.dispatchEvent(new CustomEvent('dataFiltered',{detail:{offers:this.filteredOffers,filters:this.filters}}));},resetFilters(){this.filters ={jobFamilies:[],contractTypes:[],departments:[],dateRange:null};this.filteredOffers =[...this.data.offers];window.dispatchEvent(new CustomEvent('dataFiltered',{detail:{offers:this.filteredOffers,filters:this.filters}}));},getStats(){const offers = this.filteredOffers;const withSalary = offers.filter(o => o.salaryGrossAnnual !== null);const byJobFamily ={};offers.forEach(o =>{if (!byJobFamily[o.jobFamily]){byJobFamily[o.jobFamily] ={count:0,salaries:[]};}byJobFamily[o.jobFamily].count++;if (o.salaryGrossAnnual){byJobFamily[o.jobFamily].salaries.push(o.salaryGrossAnnual);}});for (const family in byJobFamily){byJobFamily[family].salaryStats = SalaryUtils.calculateStats(byJobFamily[family].salaries);delete byJobFamily[family].salaries;}const byContractType ={};offers.forEach(o =>{const type = o.contractType || 'Non précisé';if (!byContractType[type]){byContractType[type] ={count:0,salaries:[]};}byContractType[type].count++;if (o.salaryGrossAnnual){byContractType[type].salaries.push(o.salaryGrossAnnual);}});for (const type in byContractType){byContractType[type].salaryStats = SalaryUtils.calculateStats(byContractType[type].salaries);delete byContractType[type].salaries;}const byDepartment ={};offers.forEach(o =>{const dept = o.department || 'Inconnu';if (!byDepartment[dept]){byDepartment[dept] ={name:this.data.departments[dept] || dept,count:0,salaries:[]};}byDepartment[dept].count++;if (o.salaryGrossAnnual){byDepartment[dept].salaries.push(o.salaryGrossAnnual);}});for (const dept in byDepartment){byDepartment[dept].salaryStats = SalaryUtils.calculateStats(byDepartment[dept].salaries);delete byDepartment[dept].salaries;}const bySector ={};offers.forEach(o =>{const sector = o.sector || 'Non précisé';if (!bySector[sector]){bySector[sector] ={count:0,salaries:[]};}bySector[sector].count++;if (o.salaryGrossAnnual){bySector[sector].salaries.push(o.salaryGrossAnnual);}});for (const sector in bySector){bySector[sector].salaryStats = SalaryUtils.calculateStats(bySector[sector].salaries);delete bySector[sector].salaries;}return{total:offers.length,withSalary:withSalary.length,byJobFamily,byContractType,byDepartment,bySector};},getOffersByDate(){const byDate ={};this.filteredOffers.forEach(offer =>{if (!byDate[offer.date]){byDate[offer.date] =[];}byDate[offer.date].push(offer);});return byDate;},getTopEmployers(limit = 10){const employers ={};this.filteredOffers.forEach(o =>{if (o.company){employers[o.company] =(employers[o.company] || 0)+ 1;}});return Object.entries(employers).sort((a,b)=> b[1] - a[1]).slice(0,limit).map(([name,count])=>({name,count}));},getUniqueValues(field){const values = new Set();this.data.offers.forEach(o =>{if (o[field])values.add(o[field]);});return Array.from(values).sort();}};window.DataProcessor = DataProcessor;const Charts ={instances:{},salaryType:'gross',salaryPeriod:'annual',init(){this.createJobFamilyChart();this.createContractChart();this.createSalaryContractChart();this.createSalaryFamilyChart();this.createSalaryDeptChart();},update(){const stats = DataProcessor.getStats();this.updateJobFamilyChart(stats);this.updateContractChart(stats);this.updateSalaryContractChart(stats);this.updateSalaryFamilyChart(stats);this.updateSalaryDeptChart(stats);this.updateSalaryTable(stats);},destroyChart(id){if (this.instances[id]){this.instances[id].destroy();delete this.instances[id];}},createJobFamilyChart(){const ctx = document.getElementById('chart-job-families');if (!ctx)return;this.destroyChart('jobFamilies');this.instances.jobFamilies = new Chart(ctx,{type:'doughnut',data:{labels:[],datasets:[{data:[],backgroundColor:JobClassifier.getColorPalette()}]},options:{responsive:true,plugins:{legend:{position:'right',labels:{boxWidth:12,font:{size:11}}}}}});},updateJobFamilyChart(stats){if (!this.instances.jobFamilies)return;const data = Object.entries(stats.byJobFamily).sort((a,b)=> b[1].count - a[1].count);this.instances.jobFamilies.data.labels = data.map(d => d[0]);this.instances.jobFamilies.data.datasets[0].data = data.map(d => d[1].count);this.instances.jobFamilies.data.datasets[0].backgroundColor = data.map(d => JobClassifier.getColor(d[0]));this.instances.jobFamilies.update();},createContractChart(){const ctx = document.getElementById('chart-contracts');if (!ctx)return;this.destroyChart('contracts');this.instances.contracts = new Chart(ctx,{type:'pie',data:{labels:[],datasets:[{data:[],backgroundColor:['#2563eb','#10b981','#f59e0b','#ef4444','#8b5cf6','#64748b']}]},options:{responsive:true,plugins:{legend:{position:'right',labels:{boxWidth:12,font:{size:11}}}}}});},updateContractChart(stats){if (!this.instances.contracts)return;const data = Object.entries(stats.byContractType).sort((a,b)=> b[1].count - a[1].count).slice(0,6);this.instances.contracts.data.labels = data.map(d => d[0]);this.instances.contracts.data.datasets[0].data = data.map(d => d[1].count);this.instances.contracts.update();},createSalaryContractChart(){const ctx = document.getElementById('chart-salary-contract');if (!ctx)return;this.destroyChart('salaryContract');this.instances.salaryContract = new Chart(ctx,{type:'bar',data:{labels:[],datasets:[{label:'Salaire médian',data:[],counts:[],backgroundColor:'#2563eb'}]},options:{responsive:true,indexAxis:'y',plugins:{legend:{display:false},tooltip:{callbacks:{label:ctx =>{const count = ctx.dataset.counts?.[ctx.dataIndex];return `${SalaryUtils.formatCurrency(ctx.raw)}(n=${count || 0})`;}}}},scales:{x:{ticks:{callback:v => SalaryUtils.formatCurrency(v)}},y:{border:{display:true,color:'#cbd5e1',width:2},grid:{display:false},ticks:{autoSkip:false,font:{size:11}}}}}});},updateSalaryContractChart(stats){if (!this.instances.salaryContract)return;const salaryKey = this.getSalaryKey();const data = Object.entries(stats.byContractType).filter(d => d[1].salaryStats).map(([name,d])=>({name,value:this.convertSalary(d.salaryStats.median),count:d.salaryStats.count})).sort((a,b)=> b.value - a.value).slice(0,15);this.instances.salaryContract.data.labels = data.map(d => d.name);this.instances.salaryContract.data.datasets[0].data = data.map(d => d.value);this.instances.salaryContract.data.datasets[0].counts = data.map(d => d.count);this.instances.salaryContract.data.datasets[0].label = SalaryUtils.getLabel(this.salaryType,this.salaryPeriod);this.instances.salaryContract.update();},createSalaryFamilyChart(){const ctx = document.getElementById('chart-salary-family');if (!ctx)return;this.destroyChart('salaryFamily');this.instances.salaryFamily = new Chart(ctx,{type:'bar',data:{labels:[],datasets:[{label:'Salaire médian',data:[],counts:[],backgroundColor:[]}]},options:{responsive:true,indexAxis:'y',plugins:{legend:{display:false},tooltip:{callbacks:{label:ctx =>{const count = ctx.dataset.counts?.[ctx.dataIndex];return `${SalaryUtils.formatCurrency(ctx.raw)}(n=${count || 0})`;}}}},scales:{x:{ticks:{callback:v => SalaryUtils.formatCurrency(v)}},y:{border:{display:true,color:'#cbd5e1',width:2},grid:{display:false}}}}});},updateSalaryFamilyChart(stats){if (!this.instances.salaryFamily)return;const data = Object.entries(stats.byJobFamily).filter(d => d[1].salaryStats).map(([name,d])=>({name,value:this.convertSalary(d.salaryStats.median),count:d.salaryStats.count,color:JobClassifier.getColor(name)})).sort((a,b)=> b.value - a.value);this.instances.salaryFamily.data.labels = data.map(d => d.name);this.instances.salaryFamily.data.datasets[0].data = data.map(d => d.value);this.instances.salaryFamily.data.datasets[0].counts = data.map(d => d.count);this.instances.salaryFamily.data.datasets[0].backgroundColor = data.map(d => d.color);this.instances.salaryFamily.data.datasets[0].label = SalaryUtils.getLabel(this.salaryType,this.salaryPeriod);this.instances.salaryFamily.update();},createSalaryDeptChart(){const ctx = document.getElementById('chart-salary-dept');if (!ctx)return;this.destroyChart('salaryDept');this.instances.salaryDept = new Chart(ctx,{type:'bar',data:{labels:[],datasets:[{label:'Salaire médian',data:[],counts:[],backgroundColor:'#10b981'}]},options:{responsive:true,indexAxis:'y',plugins:{legend:{display:false},tooltip:{callbacks:{label:ctx =>{const count = ctx.dataset.counts?.[ctx.dataIndex];return `${SalaryUtils.formatCurrency(ctx.raw)}(n=${count || 0})`;}}}},scales:{x:{ticks:{callback:v => SalaryUtils.formatCurrency(v)}},y:{border:{display:true,color:'#cbd5e1',width:2},grid:{display:false}}}}});},updateSalaryDeptChart(stats){if (!this.instances.salaryDept)return;const data = Object.entries(stats.byDepartment).filter(d => d[1].salaryStats).map(([code,d])=>({name:`${d.name}(${code})`,value:this.convertSalary(d.salaryStats.median),count:d.salaryStats.count})).sort((a,b)=> b.value - a.value).slice(0,15);this.instances.salaryDept.data.labels = data.map(d => d.name);this.instances.salaryDept.data.datasets[0].data = data.map(d => d.value);this.instances.salaryDept.data.datasets[0].counts = data.map(d => d.count);this.instances.salaryDept.data.datasets[0].label = SalaryUtils.getLabel(this.salaryType,this.salaryPeriod);this.instances.salaryDept.update();},updateSalaryTable(stats){const tbody = document.querySelector('#salary-summary-table tbody');if (!tbody)return;const data = Object.entries(stats.byJobFamily).sort((a,b)=> b[1].count - a[1].count);tbody.innerHTML = data.map(([family,d])=>{const s = d.salaryStats;return `
<tr>
<td><span style="color:${JobClassifier.getColor(family)};">●</span> ${family}</td>
<td>${d.count}</td>
<td>${s ? s.count:0}</td>
<td>${s ? SalaryUtils.formatCurrency(this.convertSalary(s.min)):'-'}</td>
<td><strong>${s ? SalaryUtils.formatCurrency(this.convertSalary(s.median)):'-'}</strong>${s ? ` <span class="count-badge">(n=${s.count})</span>`:''}</td>
<td>${s ? SalaryUtils.formatCurrency(this.convertSalary(s.max)):'-'}</td>
</tr>
`;}).join('');},convertSalary(value){if (!value)return null;let result = value;if (this.salaryType === 'net'){result = SalaryUtils.grossToNet(result);}if (this.salaryPeriod === 'monthly'){result = SalaryUtils.annualToMonthly(result);}return result;},getSalaryKey(){return `salary${this.salaryType.charAt(0).toUpperCase()+ this.salaryType.slice(1)}${this.salaryPeriod.charAt(0).toUpperCase()+ this.salaryPeriod.slice(1)}`;},setSalaryType(type){this.salaryType = type;this.update();},setSalaryPeriod(period){this.salaryPeriod = period;this.update();}};window.Charts = Charts;const FranceMap ={svg:null,projection:null,path:null,colorScale:null,colorMode:'count',selectedDepts:[],tooltip:null,async init(){const container = document.getElementById('france-map');if (!container)return;container.innerHTML = '';const width = container.clientWidth;const computedStyle = getComputedStyle(document.documentElement);const cssHeight = parseInt(computedStyle.getPropertyValue('--map-height-desktop').trim())|| 380;const height = Math.min(container.clientHeight || cssHeight,cssHeight);this.svg = d3.select(container).append('svg').attr('width',width).attr('height',height).attr('viewBox',`0 0 ${width}${height}`);this.tooltip = d3.select('body').append('div').attr('class','tooltip').style('opacity',0);try{const response = await fetch('data/france-topo.json');const geojson = await response.json();const padding = 20;this.projection = d3.geoMercator().fitExtent([[padding,padding],[width - padding,height - padding]],geojson);this.path = d3.geoPath().projection(this.projection);this.drawMap(geojson);this.update();}catch(error){console.error('Error loading map data:',error);container.innerHTML = '<p class="loading">Erreur de chargement de la carte</p>';}},drawMap(geojson){const g = this.svg.append('g').attr('class','departments');g.selectAll('path').data(geojson.features).enter().append('path').attr('d',this.path).attr('class','department').attr('data-code',d => d.properties.code).attr('fill','#e2e8f0').attr('stroke','#fff').attr('stroke-width',0.5).on('mouseover',(event,d)=> this.handleMouseOver(event,d)).on('mouseout',()=> this.handleMouseOut()).on('click',(event,d)=> this.handleClick(d));},update(){const stats = DataProcessor.getStats();const byDept = stats.byDepartment;let values;if (this.colorMode === 'count'){values = Object.values(byDept).map(d => d.count);}else{values = Object.values(byDept).filter(d => d.salaryStats).map(d => d.salaryStats.median);}const maxVal = Math.max(...values,1);this.colorScale = d3.scaleSequential().domain([0,maxVal]).interpolator(this.colorMode === 'count'
? d3.interpolate('#e0e7ff','#2563eb'):d3.interpolate('#fef3c7','#1d4ed8'));const self = this;this.svg.selectAll('.department').transition().duration(300).attr('fill',function (){const code = this.getAttribute('data-code');const deptData = byDept[code];if (!deptData)return '#f1f5f9';if (self.colorMode === 'count'){return self.colorScale(deptData.count);}else{return deptData.salaryStats
? self.colorScale(deptData.salaryStats.median):'#f1f5f9';}});this.updateLegend(maxVal);},updateLegend(maxVal){const legend = document.getElementById('map-legend');if (!legend)return;const title = this.colorMode === 'count' ? 'Nombre d\'offres':'Salaire médian';const isSalary = this.colorMode === 'salary';const formatVal = isSalary
? v => v === 0 ? 'NC':SalaryUtils.formatCurrency(v,true):v => Math.round(v);const steps = isSalary ?[0.25,0.5,0.75,1]:[0,0.25,0.5,0.75,1];legend.innerHTML = `
<h4>${title}</h4>
<div style="display:flex;flex-direction:column;gap:4px;">
${steps.map(p =>{const val = p * maxVal;return `
<div style="display:flex;align-items:center;gap:8px;">
<div style="width:20px;height:20px;background:${this.colorScale(val)};border-radius:2px;"></div>
<span style="font-size:12px;">${formatVal(val)}</span>
</div>
`;}).join('')}${isSalary ? `
<div style="display:flex;align-items:center;gap:8px;">
<div style="width:20px;height:20px;background:#f1f5f9;border-radius:2px;border:1px solid #e2e8f0;"></div>
<span style="font-size:12px;">NC</span>
</div>
`:''}</div>
`;},handleMouseOver(event,d){const code = d.properties.code;const name = d.properties.nom;const stats = DataProcessor.getStats();const deptData = stats.byDepartment[code];d3.select(event.target).attr('stroke','#1e40af').attr('stroke-width',2);let content = `<strong>${name}(${code})</strong><br>`;if (deptData){content += `Offres:${deptData.count}<br>`;if (deptData.salaryStats && deptData.salaryStats.median){content += `Salaire médian:${SalaryUtils.formatCurrency(deptData.salaryStats.median,true)}(n=${deptData.salaryStats.count})`;}else{content += `Salaire médian:NC`;}}else{content += 'Aucune offre';}this.tooltip
.html(content).style('left',(event.pageX + 10)+ 'px').style('top',(event.pageY - 28)+ 'px').style('opacity',1);},handleMouseOut(){this.svg.selectAll('.department').attr('stroke','#fff').attr('stroke-width',0.5);this.tooltip.style('opacity',0);},handleClick(d){const code = d.properties.code;const idx = this.selectedDepts.indexOf(code);if (idx >= 0){this.selectedDepts.splice(idx,1);}else if (this.selectedDepts.length < 3){this.selectedDepts.push(code);}this.updateComparison();},updateComparison(){const container = document.getElementById('comparison-cards');if (!container)return;if (this.selectedDepts.length === 0){container.innerHTML = '<p class="text-muted">Aucun département sélectionné</p>';return;}const stats = DataProcessor.getStats();container.innerHTML = this.selectedDepts.map(code =>{const deptData = stats.byDepartment[code];const name = DataProcessor.getData().departments[code] || code;if (!deptData){return `
<div class="comparison-card" style="background:var (--color-surface-alt);border-radius:var (--radius-md);padding:var (--spacing-md);">
<h4>${name}(${code})</h4>
<p>Aucune donnée</p>
<button onclick="FranceMap.removeFromComparison('${code}')" class="btn btn-secondary" style="margin-top:8px;padding:4px 8px;font-size:12px;">Retirer</button>
</div>
`;}return `
<div class="comparison-card" style="background:var (--color-surface-alt);border-radius:var (--radius-md);padding:var (--spacing-md);">
<h4 style="margin-bottom:8px;">${name}(${code})</h4>
<div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;font-size:14px;">
<div>
<span style="color:var (--color-text-muted);">Offres</span><br>
<strong>${deptData.count}</strong>
</div>
<div>
<span style="color:var (--color-text-muted);">Salaire médian</span><br>
<strong>${deptData.salaryStats && deptData.salaryStats.median ? `${SalaryUtils.formatCurrency(deptData.salaryStats.median,true)}<span class="count-badge">(n=${deptData.salaryStats.count})</span>`:'NC'}</strong>
</div>
</div>
<button onclick="FranceMap.removeFromComparison('${code}')" class="btn btn-secondary" style="margin-top:8px;padding:4px 8px;font-size:12px;">Retirer</button>
</div>
`;}).join('');},removeFromComparison(code){const idx = this.selectedDepts.indexOf(code);if (idx >= 0){this.selectedDepts.splice(idx,1);this.updateComparison();}},setColorMode(mode){this.colorMode = mode;this.update();}};window.FranceMap = FranceMap;const SalaryUtils ={RATES:{cadre:0.75,nonCadre:0.78},formatCurrency(value,showNA = false){if (value === null || value === undefined || value === 0){return showNA ? 'NC':'Non renseigné';}return new Intl.NumberFormat('fr-FR',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(value);},formatNumber(value){if (value === null || value === undefined)return '-';return new Intl.NumberFormat('fr-FR').format(value);},getSalary(offer,type = 'gross',period = 'annual'){if (!offer)return null;const key = `salary${type.charAt(0).toUpperCase()+ type.slice(1)}${period.charAt(0).toUpperCase()+ period.slice(1)}`;return offer[key] || null;},getLabel(type,period){const typeLabels ={gross:'Brut',net:'Net'};const periodLabels ={annual:'annuel',monthly:'mensuel'};return `${typeLabels[type]}${periodLabels[period]}`;},grossToNet(gross,isCadre = false){if (gross === null || gross === undefined)return null;const rate = isCadre ? this.RATES.cadre:this.RATES.nonCadre;return Math.round(gross * rate);},netToGross(net,isCadre = false){if (net === null || net === undefined)return null;const rate = isCadre ? this.RATES.cadre:this.RATES.nonCadre;return Math.round(net / rate);},annualToMonthly(annual,months = 12){if (annual === null || annual === undefined)return null;return Math.round(annual / months);},monthlyToAnnual(monthly,months = 12){if (monthly === null || monthly === undefined)return null;return Math.round(monthly * months);},calculateStats(salaries){const valid = salaries.filter(s => s !== null && s !== undefined && !isNaN(s));if (valid.length === 0)return null;const sorted = valid.sort((a,b)=> a - b);const len = sorted.length;return{count:len,min:sorted[0],max:sorted[len - 1],median:len % 2 === 0
?(sorted[len / 2 - 1] + sorted[len / 2])/ 2:sorted[Math.floor(len / 2)],q1:sorted[Math.floor(len * 0.25)],q3:sorted[Math.floor(len * 0.75)],mean:valid.reduce((a,b)=> a + b,0)/ len};}};window.SalaryUtils = SalaryUtils;const JobClassifier ={families:[{name:'Professeur Documentaliste',icon:'🎓',color:'#8b5cf6',description:'Enseignement et formation en documentation'},{name:'Archiviste',icon:'📚',color:'#06b6d4',description:'Gestion et conservation des archives'},{name:'Record Manager',icon:'🗃️',color:'#84cc16',description:'Gestion des documents d\'activité'},{name:'Knowledge Manager',icon:'💡',color:'#f59e0b',description:'Gestion des connaissances en entreprise'},{name:'Document Controller',icon:'✅',color:'#10b981',description:'Contrôle et suivi documentaire projet'},{name:'Veille',icon:'🔍',color:'#ec4899',description:'Veille stratégique et intelligence économique'},{name:'Médiathécaire/Bibliothécaire',icon:'📖',color:'#6366f1',description:'Animation de médiathèques et bibliothèques'},{name:'Directeur/Directrice',icon:'👔',color:'#1e40af',description:'Direction de services documentaires'},{name:'Responsable',icon:'📋',color:'#0891b2',description:'Responsabilité d\'équipe ou de projet'},{name:'Gestionnaire',icon:'🗄️',color:'#65a30d',description:'Gestion de bases de données et fonds documentaires'},{name:'Assistant(e)',icon:'🤝',color:'#f97316',description:'Assistance documentaire et administrative'},{name:'Documentaliste',icon:'📄',color:'#2563eb',description:'Documentation généraliste'}],getFamily(name){return this.families.find(f => f.name === name)|| this.families[this.families.length - 1];},getAllFamilyNames(){return this.families.map(f => f.name);},getColor(name){const family = this.getFamily(name);return family ? family.color:'#64748b';},getIcon(name){const family = this.getFamily(name);return family ? family.icon:'📄';},getColorPalette(){return this.families.map(f => f.color);}};window.JobClassifier = JobClassifier;const OffersModal ={modal:null,currentOffers:[],currentPage:1,perPage:20,context:'',sortColumn:null,sortDirection:'asc',loadedCount:0,loadItemsPerScroll:20,isLoading:false,scrollHandler:null,isMobile(){return window.innerWidth < 768;},init(){if (this.modal)return;const modalHTML = `
<div id="offers-modal" class="offers-modal-overlay" style="display:none;">
<div class="offers-modal-content">
<div class="offers-modal-header">
<div>
<h2>Offres correspondantes <span class="offers-modal-count"></span></h2>
<span class="offers-modal-context"></span>
</div>
<button class="offers-modal-close" aria-label="Fermer">&times;</button>
</div>
<div class="offers-modal-body">
<div class="offers-modal-stats"></div>
<div class="offers-table-container">
<table class="offers-table">
<thead>
<tr>
<th class="sortable" data-sort="title">Poste <span class="sort-icon"></span></th>
<th class="sortable" data-sort="company">Entreprise <span class="sort-icon"></span></th>
<th class="sortable" data-sort="department">Lieu <span class="sort-icon"></span></th>
<th class="sortable" data-sort="contractType">Contrat <span class="sort-icon"></span></th>
<th class="sortable" data-sort="salaryNetMonthly">Salaire <span class="sort-icon"></span></th>
<th></th>
</tr>
</thead>
<tbody></tbody>
</table>
</div>
<div class="offers-modal-pagination"></div>
</div>
</div>
</div>
`;document.body.insertAdjacentHTML('beforeend',modalHTML);this.modal = document.getElementById('offers-modal');this.modal.querySelector('.offers-modal-close').addEventListener('click',()=> this.hide());this.modal.addEventListener('click',(e)=>{if (e.target === this.modal)this.hide();});document.addEventListener('keydown',(e)=>{if (e.key === 'Escape' && this.modal.style.display !== 'none'){this.hide();}});this.modal.querySelectorAll('.sortable').forEach(th =>{th.addEventListener('click',()=>{const column = th.dataset.sort;this.toggleSort(column);});});},toggleSort(column){if (this.sortColumn === column){this.sortDirection = this.sortDirection === 'asc' ? 'desc':'asc';}else{this.sortColumn = column;this.sortDirection = 'asc';}this.sortOffers();this.currentPage = 1;this.renderTable();this.renderPagination();this.updateSortIcons();},sortOffers(){if (!this.sortColumn)return;const col = this.sortColumn;const dir = this.sortDirection === 'asc' ? 1:-1;this.currentOffers.sort((a,b)=>{let valA = a[col];let valB = b[col];if (valA == null && valB == null)return 0;if (valA == null)return 1;if (valB == null)return -1;if (col === 'salaryNetMonthly'){return (valA - valB)* dir;}valA = String(valA).toLowerCase();valB = String(valB).toLowerCase();return valA.localeCompare(valB,'fr')* dir;});},updateSortIcons(){this.modal.querySelectorAll('.sortable').forEach(th =>{const icon = th.querySelector('.sort-icon');if (th.dataset.sort === this.sortColumn){icon.textContent = this.sortDirection === 'asc' ? ' ▲':' ▼';th.classList.add('sorted');}else{icon.textContent = ' ⇅';th.classList.remove('sorted');}});},setupInfiniteScroll(){if (!this.isMobile())return;const container = this.modal.querySelector('.offers-table-container');if (this.scrollHandler){container.removeEventListener('scroll',this.scrollHandler);}this.scrollHandler =()=>{if (this.isLoading)return;if (this.loadedCount >= this.currentOffers.length)return;const{scrollTop,scrollHeight,clientHeight}= container;if (scrollTop + clientHeight >= scrollHeight - 100){this.loadMoreItems();}};container.addEventListener('scroll',this.scrollHandler);},loadMoreItems(){if (this.loadedCount >= this.currentOffers.length)return;this.isLoading = true;this.showLoadingIndicator();setTimeout(()=>{const tbody = this.modal.querySelector('.offers-table tbody');const start = this.loadedCount;const end = Math.min(start + this.loadItemsPerScroll,this.currentOffers.length);const newOffers = this.currentOffers.slice(start,end);tbody.insertAdjacentHTML('beforeend',this.renderOfferRows(newOffers));this.loadedCount = end;this.isLoading = false;this.hideLoadingIndicator();this.updateLoadedCount();},150);},renderOfferRows(offers){const isMobile = this.isMobile();return offers.map(offer =>{const salary = offer.salaryNetMonthly
? SalaryUtils.formatCurrency(offer.salaryNetMonthly)+ '/mois':'-';const company = offer.company || '-';const location = isMobile
?(offer.department || '-'):(offer.departmentName
? `${offer.departmentName}(${offer.department})`:(offer.department || '-'));const url = offer.url || '#';const titleContent = isMobile
? `<span class="offer-family-badge" style="background:${JobClassifier.getColor(offer.jobFamily)}20;color:${JobClassifier.getColor(offer.jobFamily)};">
${offer.jobFamily}</span>`:`<span class="offer-family-badge" style="background:${JobClassifier.getColor(offer.jobFamily)}20;color:${JobClassifier.getColor(offer.jobFamily)};">
${offer.jobFamily}</span>
<span title="${this.escapeHtml(offer.title)}">${this.truncate(offer.title,50)}</span>`;return `
<tr>
<td class="offer-title">
${titleContent}</td>
<td>${this.escapeHtml(company)}</td>
<td>${location}</td>
<td><span class="contract-badge">${this.escapeHtml(offer.contractType || '-')}</span></td>
<td>${salary}</td>
<td>
${url !== '#' ? `<a href="${url}" target="_blank" rel="noopener" class="offer-link">Voir</a>`:''}</td>
</tr>
`;}).join('');},showLoadingIndicator(){let indicator = this.modal.querySelector('.infinite-scroll-loading');if (!indicator){const container = this.modal.querySelector('.offers-table-container');container.insertAdjacentHTML('beforeend',`
<div class="infinite-scroll-loading">
<span class="loading-spinner"></span>
Chargement...
</div>
`);}},hideLoadingIndicator(){const indicator = this.modal.querySelector('.infinite-scroll-loading');if (indicator){indicator.remove();}},updateLoadedCount(){const countEl = this.modal.querySelector('.loaded-count');if (countEl){countEl.textContent = `${this.loadedCount}/ ${this.currentOffers.length}`;}},show(offers,context = ''){this.init();this.currentOffers =[...offers];this.currentPage = 1;this.loadedCount = 0;this.isLoading = false;this.context = context;this.sortColumn = null;this.sortDirection = 'asc';this.modal.querySelector('.offers-modal-context').textContent = context;const totalCount = offers.length.toLocaleString('fr-FR');this.modal.querySelector('.offers-modal-count').textContent = `(${totalCount})`;const withSalary = offers.filter(o => o.salaryNetMonthly).length;const isMobile = this.isMobile();if (isMobile){this.modal.querySelector('.offers-modal-stats').innerHTML = `
<span><strong>${offers.length}</strong> offres</span>
<span><strong>${withSalary}</strong> avec salaire</span>
<span class="loaded-count"></span>
`;}else{this.modal.querySelector('.offers-modal-stats').innerHTML = `
<span><strong>${offers.length}</strong> offres</span>
<span><strong>${withSalary}</strong> avec salaire</span>
`;}this.renderTable();this.renderPagination();this.updateSortIcons();if (isMobile){this.setupInfiniteScroll();this.updateLoadedCount();}this.modal.style.display = 'flex';document.body.style.overflow = 'hidden';},hide(){if (this.modal){this.modal.style.display = 'none';document.body.style.overflow = '';}},renderTable(){const tbody = this.modal.querySelector('.offers-table tbody');const isMobile = this.isMobile();let offersToRender;if (isMobile){const end = Math.min(this.loadItemsPerScroll,this.currentOffers.length);offersToRender = this.currentOffers.slice(0,end);this.loadedCount = end;}else{const start =(this.currentPage - 1)* this.perPage;const end = start + this.perPage;offersToRender = this.currentOffers.slice(start,end);}if (offersToRender.length === 0){tbody.innerHTML = `
<tr>
<td colspan="6" style="text-align:center;padding:2rem;color:var (--color-text-muted);">
Aucune offre correspondante
</td>
</tr>
`;return;}tbody.innerHTML = this.renderOfferRows(offersToRender);},renderPagination(){const pagination = this.modal.querySelector('.offers-modal-pagination');if (this.isMobile()){pagination.innerHTML = '';return;}const totalPages = Math.ceil(this.currentOffers.length / this.perPage);if (totalPages <= 1){pagination.innerHTML = '';return;}let html = '<div class="pagination-controls">';html += `<button class="pagination-btn" ${this.currentPage === 1 ? 'disabled':''}data-page="${this.currentPage - 1}">&laquo;Préc.</button>`;const maxVisible = 5;let startPage = Math.max(1,this.currentPage - Math.floor(maxVisible / 2));let endPage = Math.min(totalPages,startPage + maxVisible - 1);if (endPage - startPage < maxVisible - 1){startPage = Math.max(1,endPage - maxVisible + 1);}if (startPage > 1){html += `<button class="pagination-btn" data-page="1">1</button>`;if (startPage > 2)html += '<span class="pagination-ellipsis">...</span>';}for (let i = startPage;i <= endPage;i++){html += `<button class="pagination-btn ${i === this.currentPage ? 'active':''}" data-page="${i}">${i}</button>`;}if (endPage < totalPages){if (endPage < totalPages - 1)html += '<span class="pagination-ellipsis">...</span>';html += `<button class="pagination-btn" data-page="${totalPages}">${totalPages}</button>`;}html += `<button class="pagination-btn" ${this.currentPage === totalPages ? 'disabled':''}data-page="${this.currentPage + 1}">Suiv. &raquo;</button>`;html += '</div>';pagination.innerHTML = html;pagination.querySelectorAll('.pagination-btn').forEach(btn =>{btn.addEventListener('click',()=>{const page = parseInt(btn.dataset.page);if (page && page !== this.currentPage && page >= 1 && page <= totalPages){this.currentPage = page;this.renderTable();this.renderPagination();this.modal.querySelector('.offers-table-container').scrollTop = 0;}});});},truncate(text,maxLength){if (!text)return '';return text.length > maxLength ? text.substring(0,maxLength)+ '...':text;},escapeHtml(text){if (!text)return '';const div = document.createElement('div');div.textContent = text;return div.innerHTML;}};window.OffersModal = OffersModal;const BottomSheet =(function (){let sheetEl = null;let overlayEl = null;let contentEl = null;let closeBtn = null;let resetBtn = null;let applyBtn = null;let filterBtn = null;let filterBadge = null;let isOpen = false;let startY = 0;let currentY = 0;let isDragging = false;let departments ={};let pendingFilters ={jobFamilies:[],contractTypes:[],departments:[]};function init(){sheetEl = document.getElementById('bottom-sheet');overlayEl = document.getElementById('bottom-sheet-overlay');contentEl = document.getElementById('bottom-sheet-content');closeBtn = document.getElementById('bottom-sheet-close');resetBtn = document.getElementById('bottom-sheet-reset');applyBtn = document.getElementById('bottom-sheet-apply');filterBtn = document.getElementById('mobile-filter-btn');filterBadge = document.getElementById('filter-badge');if (!sheetEl){console.warn('[BottomSheet] Sheet element not found');return;}const sidebar = document.querySelector('.sidebar .filter-section');const checkboxes = sidebar?.querySelectorAll('input[type="checkbox"]');if (!checkboxes || checkboxes.length === 0){console.warn('[BottomSheet] Filters not yet populated,deferring initialization');window.addEventListener('filtersReady',()=>{console.log('[BottomSheet] Filters ready,refreshing content');refreshFilters();},{once:true});return;}cloneFilterContent();filterBtn?.addEventListener('click',open);closeBtn?.addEventListener('click',close);overlayEl?.addEventListener('click',close);resetBtn?.addEventListener('click',resetFilters);applyBtn?.addEventListener('click',applyFilters);sheetEl.addEventListener('touchstart',handleTouchStart,{passive:true});sheetEl.addEventListener('touchmove',handleTouchMove,{passive:false});sheetEl.addEventListener('touchend',handleTouchEnd,{passive:true});document.addEventListener('keydown',(e)=>{if (e.key === 'Escape' && isOpen){close();}});window.addEventListener('dataFiltered',updateBadge);console.log('[BottomSheet] Initialized');}function cloneFilterContent(){const sidebar = document.querySelector('.sidebar .filter-section');if (!sidebar || !contentEl)return;const filterGroups = sidebar.querySelectorAll('.filter-group');filterGroups.forEach((group)=>{const clone = group.cloneNode(true);const originalId = clone.querySelector('[id]')?.id;if (originalId){clone.querySelectorAll('[id]').forEach((el)=>{el.id = `bottom-sheet-${el.id}`;});clone.querySelectorAll('[for]').forEach((el)=>{el.setAttribute('for',`bottom-sheet-${el.getAttribute('for')}`);});}contentEl.appendChild(clone);});setupCheckboxListeners();}function setupCheckboxListeners(){if (!contentEl)return;const jobFamilyContainer = contentEl.querySelector('[id*="filter-job-family"]');if (jobFamilyContainer){jobFamilyContainer.addEventListener('change',(e)=>{if (e.target.type === 'checkbox'){const value = e.target.value;if (e.target.checked){if (!pendingFilters.jobFamilies.includes(value)){pendingFilters.jobFamilies.push(value);}}else{pendingFilters.jobFamilies = pendingFilters.jobFamilies.filter(v => v !== value);}}});}const contractContainer = contentEl.querySelector('[id*="filter-contract"]');if (contractContainer){contractContainer.addEventListener('change',(e)=>{if (e.target.type === 'checkbox'){const value = e.target.value;if (e.target.checked){if (!pendingFilters.contractTypes.includes(value)){pendingFilters.contractTypes.push(value);}}else{pendingFilters.contractTypes = pendingFilters.contractTypes.filter(v => v !== value);}}});}setupDepartmentAutocomplete();}function setupDepartmentAutocomplete(){const input = contentEl.querySelector('[id*="dept-search"]');const dropdown = contentEl.querySelector('[id*="dept-dropdown"]');const chipsContainer = contentEl.querySelector('[id*="dept-chips"]');if (!input || !dropdown || !chipsContainer)return;let highlightedIndex = -1;const renderChips =()=>{chipsContainer.innerHTML = pendingFilters.departments.map(code =>{const name = departments[code] || code;return `
<span class="dept-chip" data-code="${code}">
${name}(${code})<button class="dept-chip-remove" aria-label="Retirer ${name}">&times;</button>
</span>
`;}).join('');chipsContainer.querySelectorAll('.dept-chip-remove').forEach(btn =>{btn.addEventListener('click',(e)=>{const chip = e.target.closest('.dept-chip');const code = chip.dataset.code;pendingFilters.departments = pendingFilters.departments.filter(d => d !== code);renderChips();});});};const renderDropdown =(query)=>{if (!query || query.length < 1){dropdown.hidden = true;return;}const deptEntries = Object.entries(departments);const matches = deptEntries
.filter(([code,name])=>
!pendingFilters.departments.includes(code)&&(code.toLowerCase().includes(query)||
name.toLowerCase().includes(query))).slice(0,8);if (matches.length === 0){dropdown.innerHTML = '<div class="dept-no-results">Aucun résultat</div>';}else{dropdown.innerHTML = matches.map(([code,name],i)=> `
<div class="dept-option ${i === highlightedIndex ? 'highlighted':''}"
data-code="${code}"
data-index="${i}"
role="option">
<span class="dept-option-code">${code}</span>
${name}</div>
`).join('');}dropdown.hidden = false;dropdown.querySelectorAll('.dept-option').forEach(opt =>{opt.addEventListener('click',()=> selectDept(opt.dataset.code));});};const selectDept =(code)=>{if (!pendingFilters.departments.includes(code)){pendingFilters.departments.push(code);}input.value = '';dropdown.hidden = true;highlightedIndex = -1;renderChips();};let debounceTimer;input.addEventListener('input',(e)=>{clearTimeout(debounceTimer);debounceTimer = setTimeout(()=>{highlightedIndex = -1;renderDropdown(e.target.value.toLowerCase());},100);});input.addEventListener('keydown',(e)=>{const options = dropdown.querySelectorAll('.dept-option');if (e.key === 'ArrowDown'){e.preventDefault();highlightedIndex = Math.min(highlightedIndex + 1,options.length - 1);updateHighlight(options);}else if (e.key === 'ArrowUp'){e.preventDefault();highlightedIndex = Math.max(highlightedIndex - 1,0);updateHighlight(options);}else if (e.key === 'Enter' && highlightedIndex >= 0){e.preventDefault();const code = options[highlightedIndex]?.dataset.code;if (code)selectDept(code);}else if (e.key === 'Escape'){dropdown.hidden = true;highlightedIndex = -1;}});const updateHighlight =(options)=>{options.forEach((opt,i)=>{opt.classList.toggle('highlighted',i === highlightedIndex);});};contentEl.addEventListener('click',(e)=>{if (!e.target.closest('.dept-autocomplete')){dropdown.hidden = true;}});input.addEventListener('focus',()=>{if (input.value.length >= 1){renderDropdown(input.value.toLowerCase());}});contentEl._renderDeptChips = renderChips;}function syncWithSidebar(){const currentFilters = DataProcessor.filters;pendingFilters ={jobFamilies:[...currentFilters.jobFamilies],contractTypes:[...currentFilters.contractTypes],departments:[...currentFilters.departments]};if (!contentEl)return;contentEl.querySelectorAll('[id$="filter-job-family"] input[type="checkbox"]').forEach((cb)=>{cb.checked = pendingFilters.jobFamilies.includes(cb.value);});contentEl.querySelectorAll('[id$="filter-contract"] input[type="checkbox"]').forEach((cb)=>{cb.checked = pendingFilters.contractTypes.includes(cb.value);});if (contentEl._renderDeptChips){contentEl._renderDeptChips();}}function open(){if (!sheetEl || !overlayEl)return;syncWithSidebar();isOpen = true;sheetEl.hidden = false;overlayEl.hidden = false;requestAnimationFrame(()=>{sheetEl.classList.add('open');overlayEl.classList.add('visible');});document.body.style.overflow = 'hidden';closeBtn?.focus();}function close(){if (!sheetEl || !overlayEl)return;isOpen = false;sheetEl.classList.remove('open');overlayEl.classList.remove('visible');setTimeout(()=>{sheetEl.hidden = true;overlayEl.hidden = true;},300);document.body.style.overflow = '';filterBtn?.focus();}function handleTouchStart(e){const handle = sheetEl.querySelector('.bottom-sheet-handle');const header = sheetEl.querySelector('.bottom-sheet-header');if (!handle?.contains(e.target)&& !header?.contains(e.target))return;isDragging = true;startY = e.touches[0].clientY;currentY = startY;sheetEl.style.transition = 'none';}function handleTouchMove(e){if (!isDragging)return;currentY = e.touches[0].clientY;const deltaY = currentY - startY;if (deltaY > 0){e.preventDefault();sheetEl.style.transform = `translateY(${deltaY}px)`;}}function handleTouchEnd(){if (!isDragging)return;isDragging = false;sheetEl.style.transition = '';sheetEl.style.transform = '';const deltaY = currentY - startY;if (deltaY > 100){close();}}function resetFilters(){pendingFilters ={jobFamilies:[],contractTypes:[],departments:[]};if (contentEl){contentEl.querySelectorAll('input[type="checkbox"]').forEach((cb)=>{cb.checked = false;});}const deptSearch = contentEl?.querySelector('[id*="dept-search"]');const deptDropdown = contentEl?.querySelector('[id*="dept-dropdown"]');if (deptSearch){deptSearch.value = '';}if (deptDropdown){deptDropdown.hidden = true;}if (contentEl._renderDeptChips){contentEl._renderDeptChips();}}function applyFilters(){DataProcessor.applyFilters(pendingFilters);syncSidebarWithPending();close();}function syncSidebarWithPending(){document.querySelectorAll('#filter-job-family input[type="checkbox"]').forEach((cb)=>{cb.checked = pendingFilters.jobFamilies.includes(cb.value);});document.querySelectorAll('#filter-contract input[type="checkbox"]').forEach((cb)=>{cb.checked = pendingFilters.contractTypes.includes(cb.value);});if (window.App){App.selectedDepts = new Set(pendingFilters.departments);const chipsContainer = document.getElementById('dept-chips');if (chipsContainer){chipsContainer.innerHTML = pendingFilters.departments.map(code =>{const name = departments[code] || code;return `
<span class="dept-chip" data-code="${code}">
${name}(${code})<button class="dept-chip-remove" aria-label="Retirer ${name}">&times;</button>
</span>
`;}).join('');chipsContainer.querySelectorAll('.dept-chip-remove').forEach(btn =>{btn.addEventListener('click',(e)=>{const chip = e.target.closest('.dept-chip');const code = chip.dataset.code;App.selectedDepts.delete (code);chip.remove();App.applyFilters();});});}}}function updateBadge(){if (!filterBadge)return;const filters = DataProcessor.filters;const count = filters.jobFamilies.length +
filters.contractTypes.length +
filters.departments.length;if (count > 0){filterBadge.textContent = count;filterBadge.hidden = false;}else{filterBadge.hidden = true;}}function isSheetOpen(){return isOpen;}function refreshFilters(){if (!contentEl)return;contentEl.innerHTML = '';cloneFilterContent();console.log('[BottomSheet] Filters refreshed');}function setDepartmentData(deptData){departments = deptData ||{};console.log('[BottomSheet] Department data set:',Object.keys(departments).length,'departments');}return{init,open,close,isOpen:isSheetOpen,updateBadge,refreshFilters,setDepartmentData};})();window.BottomSheet = BottomSheet;const Timeline ={chart:null,init(){const ctx = document.getElementById('chart-timeline');if (!ctx)return;if (this.chart){this.chart.destroy();}this.chart = new Chart(ctx,{type:'line',data:{labels:[],datasets:[{label:'Nombre d\'offres',data:[],borderColor:'#2563eb',backgroundColor:'rgba(37,99,235,0.1)',fill:true,tension:0.3}]},options:{responsive:true,interaction:{intersect:false,mode:'index'},plugins:{legend:{display:false},tooltip:{callbacks:{title:ctx =>{const date = new Date(ctx[0].label);return date.toLocaleDateString('fr-FR',{day:'numeric',month:'long',year:'numeric'});}}}},scales:{x:{ticks:{callback:function (value,index){const date = new Date(this.getLabelForValue(value));return date.toLocaleDateString('fr-FR',{month:'short'});},maxTicksLimit:12}},y:{beginAtZero:true,ticks:{precision:0}}}}});this.update();},update(){if (!this.chart)return;const data = DataProcessor.getData();if (!data)return;const offersByDate = DataProcessor.getOffersByDate();const dates = data.dates.sort();const counts = dates.map(date =>{const offers = offersByDate[date] ||[];return offers.length;});this.chart.data.labels = dates;this.chart.data.datasets[0].data = counts;this.chart.update();}};window.Timeline = Timeline;const DossierMetiers ={init(){this.update();},update(){const container = document.getElementById('dossier-grid');if (!container)return;const stats = DataProcessor.getStats();const families = Object.entries(stats.byJobFamily).sort((a,b)=> b[1].count - a[1].count);container.innerHTML = families.map(([family,data])=>{const info = JobClassifier.getFamily(family);const salary = data.salaryStats;const employers = this.getTopEmployersForFamily(family);return `
<div class="dossier-card">
<h4>${info.icon}${family}</h4>
<p style="color:var (--color-text-muted);font-size:14px;margin-bottom:16px;">${info.description}</p>
<div class="dossier-stats">
<div class="dossier-stat">
<span class="dossier-stat-value">${data.count}</span>
<span class="dossier-stat-label">Offres</span>
</div>
<div class="dossier-stat">
<span class="dossier-stat-value">${salary ? salary.count:0}</span>
<span class="dossier-stat-label">Avec salaire</span>
</div>
</div>
${salary ? `
<div class="dossier-salary">
<h5>Fourchette de salaires(brut annuel)</h5>
<div class="salary-range">
<span>${SalaryUtils.formatCurrency(salary.min)}</span>
<div class="salary-bar"></div>
<span>${SalaryUtils.formatCurrency(salary.max)}</span>
</div>
<p style="text-align:center;margin-top:8px;font-size:14px;">
Médiane:<strong style="color:var (--color-primary);">${SalaryUtils.formatCurrency(salary.median)}</strong>
</p>
</div>
`:`
<div class="dossier-salary">
<p style="color:var (--color-text-muted);font-style:italic;">Données salariales insuffisantes</p>
</div>
`}${employers.length > 0 ? `
<div class="dossier-employers">
<h5>Principaux recruteurs</h5>
<div class="employer-tags">
${employers.slice(0,5).map(e => `
<span class="employer-tag">${e.name}(${e.count})</span>
`).join('')}</div>
</div>
`:''}<button class="btn btn-cta" data-job-family="${family}">Découvrir la typologie de ces offres(passées)</button>
</div>
`;}).join('');container.querySelectorAll('.btn-cta[data-job-family]').forEach(btn =>{btn.addEventListener('click',()=>{const family = btn.dataset.jobFamily;const offers = DataProcessor.getOffers(true).filter(o => o.jobFamily === family);OffersModal.show(offers,`Famille:${family}`);});});},getTopEmployersForFamily(family){const offers = DataProcessor.getOffers().filter(o => o.jobFamily === family);const employers ={};offers.forEach(o =>{if (o.company){employers[o.company] =(employers[o.company] || 0)+ 1;}});return Object.entries(employers).sort((a,b)=> b[1] - a[1]).slice(0,5).map(([name,count])=>({name,count}));}};window.DossierMetiers = DossierMetiers;const ErrorToast =(function (){let container = null;const defaultDuration = 5000;const messages ={network:"Une erreur réseau s'est produite. Veuillez vérifier votre connexion.",data:"Impossible de charger les données. Veuillez réessayer.",filter:"Erreur lors de l'application des filtres.",unknown:"Une erreur inattendue s'est produite."};function init(){if (container)return;container = document.createElement('div');container.className = 'error-toast-container';container.setAttribute('role','alert');container.setAttribute('aria-live','polite');document.body.appendChild(container);console.log('[ErrorToast] Initialized');}function show(message,duration = defaultDuration){if (!container)init();const toast = document.createElement('div');toast.className = 'error-toast';toast.innerHTML = `
<div class="error-toast-icon" aria-hidden="true">
<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
<circle cx="12" cy="12" r="10"></circle>
<line x1="12" y1="8" x2="12" y2="12"></line>
<line x1="12" y1="16" x2="12.01" y2="16"></line>
</svg>
</div>
<div class="error-toast-content">
<strong>Oups,nous sommes désolés !</strong>
<p>${escapeHtml(message)}</p>
</div>
<button class="error-toast-close" aria-label="Fermer">
<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
<line x1="18" y1="6" x2="6" y2="18"></line>
<line x1="6" y1="6" x2="18" y2="18"></line>
</svg>
</button>
`;const closeBtn = toast.querySelector('.error-toast-close');closeBtn.addEventListener('click',()=> dismiss(toast));container.appendChild(toast);requestAnimationFrame(()=>{toast.classList.add('visible');});if (duration > 0){setTimeout(()=> dismiss(toast),duration);}return toast;}function dismiss(toast){if (!toast || !toast.parentNode)return;toast.classList.remove('visible');toast.classList.add('dismissing');setTimeout(()=>{if (toast.parentNode){toast.remove();}},300);}function showNetworkError(duration = 8000){return show(messages.network,duration);}function showDataError(duration = 8000){return show(messages.data,duration);}function showFilterError(duration = 5000){return show(messages.filter,duration);}function showUnknownError(duration = 5000){return show(messages.unknown,duration);}function escapeHtml(text){const div = document.createElement('div');div.textContent = text;return div.innerHTML;}return{init,show,dismiss,showNetworkError,showDataError,showFilterError,showUnknownError,messages};})();window.ErrorToast = ErrorToast;const UpdateToast =(function (){const COUNTDOWN_SECONDS = 5;let toastEl = null;let countdownEl = null;let reloadBtn = null;let dismissBtn = null;let countdownInterval = null;let remainingSeconds = COUNTDOWN_SECONDS;function init(){toastEl = document.getElementById('update-toast');countdownEl = document.getElementById('update-countdown');reloadBtn = document.getElementById('update-reload');dismissBtn = document.getElementById('update-dismiss');if (!toastEl){console.warn('[UpdateToast] Toast element not found');return;}reloadBtn?.addEventListener('click',()=>{reload();});dismissBtn?.addEventListener('click',()=>{dismiss();});window.addEventListener('swUpdated',(event)=>{console.log('[UpdateToast] Service Worker updated',event.detail);show();});console.log('[UpdateToast] Initialized');}function show(){if (!toastEl)return;toastEl.hidden = false;toastEl.classList.add('visible');remainingSeconds = COUNTDOWN_SECONDS;updateCountdownText();countdownInterval = setInterval(()=>{remainingSeconds--;updateCountdownText();if (remainingSeconds <= 0){reload();}},1000);}function updateCountdownText(){if (countdownEl){countdownEl.textContent = `Rechargement dans ${remainingSeconds}s...`;}}function dismiss(){if (countdownInterval){clearInterval(countdownInterval);countdownInterval = null;}if (toastEl){toastEl.classList.remove('visible');setTimeout(()=>{toastEl.hidden = true;},300);}}function reload(){if (countdownInterval){clearInterval(countdownInterval);countdownInterval = null;}window.location.reload();}function isVisible(){return toastEl && !toastEl.hidden;}return{init,show,dismiss,reload,isVisible};})();window.UpdateToast = UpdateToast;const SwipeNavigation =(function (){const SWIPE_THRESHOLD = 50;const SWIPE_VELOCITY_THRESHOLD = 0.3;const MAX_VERTICAL_RATIO = 0.75;let contentArea = null;let tabButtons = null;let currentTabIndex = 0;let startX = 0;let startY = 0;let startTime = 0;let isTracking = false;let isSwiping = false;const TAB_ORDER =['overview','salaries','map','sectors','dossier'];function init(){contentArea = document.querySelector('.content-area');tabButtons = document.querySelectorAll('.tab-btn');if (!contentArea){console.warn('[SwipeNavigation] Content area not found');return;}updateCurrentTabIndex();contentArea.addEventListener('touchstart',handleTouchStart,{passive:true});contentArea.addEventListener('touchmove',handleTouchMove,{passive:false});contentArea.addEventListener('touchend',handleTouchEnd,{passive:true});tabButtons.forEach((btn,index)=>{btn.addEventListener('click',()=>{currentTabIndex = index;});});console.log('[SwipeNavigation] Initialized');}function updateCurrentTabIndex(){const activeTab = document.querySelector('.tab-btn.active');if (activeTab){const tabName = activeTab.dataset.tab;currentTabIndex = TAB_ORDER.indexOf(tabName);if (currentTabIndex === -1)currentTabIndex = 0;}}function handleTouchStart(e){if (window.BottomSheet && BottomSheet.isOpen())return;const target = e.target;if (isInteractiveElement(target))return;if (isChartOrMap(target))return;startX = e.touches[0].clientX;startY = e.touches[0].clientY;startTime = Date.now();isTracking = true;isSwiping = false;}function handleTouchMove(e){if (!isTracking)return;const currentX = e.touches[0].clientX;const currentY = e.touches[0].clientY;const deltaX = currentX - startX;const deltaY = currentY - startY;const absX = Math.abs(deltaX);const absY = Math.abs(deltaY);if (absY > absX * MAX_VERTICAL_RATIO && absY > 10){isTracking = false;return;}if (isInHorizontallyScrollableTable(e.target)){isTracking = false;return;}if (absX > 10){isSwiping = true;e.preventDefault();}}function handleTouchEnd(e){if (!isTracking || !isSwiping){isTracking = false;return;}const endX = e.changedTouches[0].clientX;const endY = e.changedTouches[0].clientY;const endTime = Date.now();const deltaX = endX - startX;const deltaY = endY - startY;const deltaTime = endTime - startTime;const velocity = Math.abs(deltaX)/ deltaTime;const isValidSwipe =(Math.abs(deltaX)> SWIPE_THRESHOLD &&
velocity > SWIPE_VELOCITY_THRESHOLD &&
Math.abs(deltaY)< Math.abs(deltaX)* MAX_VERTICAL_RATIO);if (isValidSwipe){if (deltaX < 0){navigateToNextTab();}else{navigateToPreviousTab();}}isTracking = false;isSwiping = false;}function isInteractiveElement(element){const interactiveTags =['BUTTON','A','INPUT','SELECT','TEXTAREA'];const interactiveRoles =['button','link','checkbox','radio','slider'];let current = element;while (current && current !== contentArea){if (interactiveTags.includes(current.tagName))return true;const role = current.getAttribute('role');if (role && interactiveRoles.includes(role))return true;if (current.onclick || current.hasAttribute('data-clickable'))return true;current = current.parentElement;}return false;}function isInHorizontallyScrollableTable(element){let current = element;while (current && current !== contentArea){if (current.classList &&(current.classList.contains('salary-table-container')|| current.classList.contains('horizontal-scroll-container'))){if (current.scrollWidth > current.clientWidth){return true;}}if (current.tagName === 'TABLE'){const container = current.parentElement;if (container && container.scrollWidth > container.clientWidth){return true;}}current = current.parentElement;}return false;}function isChartOrMap(element){let current = element;while (current && current !== contentArea){if (current.tagName === 'CANVAS')return true;if (current.tagName === 'SVG' || current.tagName === 'svg')return true;if (current.id === 'france-map' || current.id === 'sector-treemap')return true;if (current.classList.contains('chart-card')){return false;}current = current.parentElement;}return false;}function navigateToNextTab(){if (currentTabIndex < TAB_ORDER.length - 1){currentTabIndex++;activateTab(TAB_ORDER[currentTabIndex]);}}function navigateToPreviousTab(){if (currentTabIndex > 0){currentTabIndex--;activateTab(TAB_ORDER[currentTabIndex]);}}function activateTab(tabName){const tabBtn = document.querySelector(`.tab-btn[data-tab="${tabName}"]`);if (tabBtn){tabBtn.click();tabBtn.scrollIntoView({behavior:'smooth',block:'nearest',inline:'center'});}}function getCurrentTab(){return TAB_ORDER[currentTabIndex];}function setCurrentTab(tabName){const index = TAB_ORDER.indexOf(tabName);if (index !== -1){currentTabIndex = index;}}return{init,getCurrentTab,setCurrentTab,navigateToNextTab,navigateToPreviousTab};})();window.SwipeNavigation = SwipeNavigation;const HashRouter =(function (){const ENABLE_HASH_ROUTING = true;const ENABLE_PATH_TO_HASH_REDIRECT = false;const ENABLE_HASH_TO_PATH_REDIRECT = false;let currentPath = '/';let currentParams ={};let listeners =[];function init(){if (!ENABLE_HASH_ROUTING){console.log('[HashRouter] Hash routing disabled');return;}window.addEventListener('hashchange',handleHashChange);parseCurrentHash();dispatchRouteChanged();console.log('[HashRouter] Initialized');}function parseCurrentHash(){const hash = window.location.hash;if (!hash || hash === '#' || hash === '#/'){currentPath = '/';currentParams ={};return;}let hashContent = hash.substring(1);const[pathPart,queryPart] = hashContent.split('?');currentPath = pathPart || '/';if (queryPart){currentParams = Object.fromEntries(new URLSearchParams(queryPart));}else{currentParams ={};}}function handleHashChange(){parseCurrentHash();dispatchRouteChanged();}function navigateTo(path,params ={}){let uri = path;if (!path.startsWith('/'))uri = '/' + path;let hashUri = '#' + uri;if (Object.keys(params).length > 0){const queryString = new URLSearchParams(params).toString();hashUri += '?' + queryString;}window.location.hash = hashUri;parseCurrentHash();dispatchRouteChanged();}function getPath(){return currentPath;}function getParams(){return{...currentParams};}function getParam(key,defaultValue = null){return currentParams[key] ?? defaultValue;}function setParam(key,value){const newParams ={...currentParams,[key]:value};navigateTo(currentPath,newParams);}function removeParam(key){const newParams ={...currentParams};delete newParams[key];navigateTo(currentPath,newParams);}function isPath(testPath){return currentPath === testPath;}function isPathLike(testPath){return currentPath.startsWith(testPath);}function onRouteChanged(callback){listeners.push(callback);return ()=>{listeners = listeners.filter(cb => cb !== callback);};}function dispatchRouteChanged(){listeners.forEach(callback =>{try{callback({path:currentPath,params:currentParams,hash:window.location.hash});}catch(error){console.error('[HashRouter] Callback error:',error);}});window.dispatchEvent(new CustomEvent('hashRouteChanged',{detail:{path:currentPath,params:currentParams}}));}function handlePathRoute(path,params ={}){if (ENABLE_PATH_TO_HASH_REDIRECT && !path.includes('#')){console.log('[HashRouter] Redirecting path to hash:',path);navigateTo(path,params);return false;}return true;}function getShareableUrl(){const baseUrl = window.location.origin + window.location.pathname;const hash = window.location.hash;return baseUrl + hash;}function restoreStateFromUrl(){parseCurrentHash();return{path:currentPath,params:currentParams};}function replaceState(path,params ={}){let uri = path;if (!path.startsWith('/'))uri = '/' + path;let hashUri = '#' + uri;if (Object.keys(params).length > 0){const queryString = new URLSearchParams(params).toString();hashUri += '?' + queryString;}window.history.replaceState(null,'',window.location.pathname + hashUri);parseCurrentHash();dispatchRouteChanged();}return{init,navigateTo,getPath,getParams,getParam,setParam,removeParam,isPath,isPathLike,onRouteChanged,getShareableUrl,restoreStateFromUrl,replaceState,handlePathRoute};})();if (document.readyState === 'loading'){document.addEventListener('DOMContentLoaded',HashRouter.init);}else{HashRouter.init();}window.HashRouter = HashRouter;
// Initialize app after all modules are loaded
if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",()=>App.init())}else{App.init()}
